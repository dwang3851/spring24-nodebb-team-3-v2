name: mutation testing

on:
  push:
    branches:
      - main
      - stryker

jobs:
  core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npx stryker run
        env:
          CI: true
